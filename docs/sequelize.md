# Guia de instalação do ORM Sequelize

## Instale o sequelize

```shell
npm install --save sequelize
```

## Instale o driver do banco de dados (Ex: Postgres)

```shell
npm install --save pg pg-hstore # Postgres
```
